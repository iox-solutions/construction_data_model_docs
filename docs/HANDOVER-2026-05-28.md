# Handover — IOX session 2026-05-28

This document captures the state of IOX at the end of the 2026-05-28 work block and the decisions awaiting you before the next push of progress.

Audience: you (in a later session), me (when I next pick this up).

---

## TL;DR

- Schema unchanged at v1.13. Tag `v1.13.0` now exists at the original schema commit (`9ad97d2`).
- The full **standards layer is built and committed** on top: governance, generated types, schema lint, CI, dev DB, RFC scaffolding, module catalogue, docs front-door.
- Three commits sit ahead of `origin/main`. **Nothing pushed.**
- Three draft RFCs sit in `docs/rfcs/` awaiting your decisions. Each contains options and a recommendation, but no decision is recorded.
- The lint correctly reports 3 errors against the current schema (all pre-existing, all covered by the RFCs above).

---

## Where the repo stands

```
construction_data_model_docs/  (this directory — the git root)
├── README.md                 ← rewritten as engineer front door
├── ENGINEER_HANDBOOK.md      ← consumer pitch
├── GOVERNANCE.md             ← roles, change classes, RFC flow
├── CHANGELOG.md              ← Unreleased: known issues from lint
├── docker-compose.yml        ← `npm run db:up` → local Postgres on :5432
├── package.json              ← npm script surface
├── .github/workflows/ci.yml  ← 3-job CI: lint / typecheck / migration-apply
├── .githooks/pre-commit      ← optional, activate via `npm run hooks:install`
├── docs/
│   ├── LOCAL_DEV.md DEPLOY.md MODULES.md POLYMORPHIC_REFS.md
│   └── rfcs/{0000-template,0001-orphans,0002-passwordHash,0003-cross-module-FKs}.md
├── iox-types/                ← @iox/types, generated from migration SQL
└── tools/                    ← parse-schema, generate-types,
                                 generate-modules-doc, schema-lint, migration-test.sh
```

```
HEAD = 4822105  feat: complete IOX standards layer — CI, npm surface, RFCs, docs
       98d6983  feat: introduce IOX standards layer — governance, types, dev DB, lint
       9f16f40  docs: check in pre-existing schema docs, scripts, and seed data
       9ad97d2  ← v1.13.0 tag (schema baseline, unchanged)
```

Verified locally before handover:

```
node tools/generate-types.mjs         → 84 tables, 139 FKs ✓
node tools/generate-modules-doc.mjs   → 84 tables in 6 modules, 3 orphans, 6 views ✓
node tools/schema-lint.mjs            → 3 errors, 47 warnings (as expected) ✓
npx tsc -p iox-types/tsconfig.json --noEmit  → clean ✓
```

---

## Decisions awaiting you

Six items, three categories.

### A. The three RFCs

Each in `docs/rfcs/`. Each contains the problem, options analysis, recommendation, migration path. You decide; I draft the merge PR.

| RFC | Subject | Recommendation | Effort to land |
|---|---|---|---|
| **0001** | Resolve three orphan tables (`ProjectTeam`, `CommunicationProtocol`, `QASheet`) | Assign `ProjectTeam` to Core; assign `CommunicationProtocol` to ProcureX; delete `QASheet` + `Checklist` + `ChecklistItem` stubs | Config-only for #1–#2; one DROP migration for #3. Half a session. |
| **0002** | Rename `User.password` → `User.passwordHash`; document hashing contract | Land the rename; **request waiver of the one-minor deprecation window** because no consumer exists yet | One ALTER, seed update, regen. Half a session. |
| **0003** | Resolve the two cross-module FK violations | Move `CertifiedPayment` Core → core; move `CostPlanElement` core → planx. Pure `module-mapping.json` config change. | Config-only. 30 minutes. |

If accepted as-is, all three together are well under a session of work and close every current lint error.

### B. Operational authorisations

| Item | Why it's blocked | What I need from you |
|---|---|---|
| Push commits to `origin` | Hard-to-reverse, affects shared state | "yes push" — and whether to push the RFC fixes before or after the standards layer commits. Note CI will fail at schema-lint on first run unless RFCs 0002+0003 land first (or you accept a temporary `--warn-only` patch on CI). |
| Connect to / inspect Azure DB | I don't have credentials and shouldn't | If anything is deployed, confirm so the migration files I'd write for RFC 0002 include a safe path |
| Name module owners | Decision only you can make | "Core owner = X, ProcureX owner = Y, ..." |

### C. Forward direction (after the above)

| Item | What it unlocks | What I need from you |
|---|---|---|
| `create-iox-app` scaffold | First-mover product team can be productive in minutes | Stack preference: Next.js / Remix / plain Node? Prisma / Kysely / Drizzle? |
| First product consumer scoping | The schema starts being pressure-tested by real workloads | Confirm ProcureX as the candidate (37 tables, most coverage). I can scope a Phase 1 build plan when you say so |
| Unify ParametriX versioning (`v0.x` → align with Core) | Coherent SemVer story across modules | Whether anything is deployed (see B above) |

---

## What you should NOT do next session

- Re-pitch the standards layer. It's built. Re-litigating it is wasted breath.
- Rebuild what's in `tools/`. The generators are zero-dep, work, and are committed.
- Skip the RFCs and start the create-iox-app scaffold. The first consumer will hit the schema warts the RFCs address. Land the RFCs first.

---

## Quick verification when you reopen

```bash
cd construction_data_model_docs

git log --oneline -5            # confirm HEAD is 4822105 or descendant
git status                      # should be clean
node tools/schema-lint.mjs      # 3 errors, 47 warnings — unchanged means nothing has rotted
npm test                        # generate:check + lint + typecheck
```

If any of those produce a surprise, investigate before doing anything else.

---

## Sources

- `CHANGELOG.md` `[Unreleased]` — known issues list
- `docs/rfcs/000{1,2,3}-*.md` — the three pending decisions
- `GOVERNANCE.md` — change classes and RFC flow
- `.claude/memory/standards-layer-built.md` — auto-loaded into Claude's next session
- `.claude/memory/iox-handover-2026-05-28.md` — fuller narrative
